/* ============================================================
   🔌 API 配置文件 — 修改这一个文件切换数据模式
   ------------------------------------------------------------
   三行改一行，零代码切换：

   // A. 纯前端模式（默认）：直接读 index.html 里的 CONFIG
   API_MODE = 'config';

   // B. 本地存储模式：数据存浏览器 localStorage
   API_MODE = 'local';

   // C. 远程后端模式：从 admin/server.js 拉数据
   API_MODE = 'remote';
   API_BASE = 'http://localhost:3000';  // 改成你的域名
   ------------------------------------------------------------

   部署到 GitHub Pages 时：
   - 主页是静态的，无法直连 localhost
   - 方案1：用 'config' 模式（纯静态，手动改 CONFIG）
   - 方案2：用 'remote' 模式 + 把 admin 部署到有 Node.js 的服务器
   - 方案3：本地编辑后用 'local' 模式导出 JSON，手动更新

   部署到自有服务器时：
   - 主页 + admin 都放同一个服务器
   - API_BASE 设为同域地址（或留空字符串表示同源）
   ============================================================ */

// ====== 修改这里 ======
const API_MODE = 'remote';          // 'config' | 'local' | 'remote'
const API_BASE = '';                // 远程模式填后端地址，同源留空
const API_TOKEN = '';               // 主页一般不需要 token（只读）
// ======================

/* 这个文件会在 index.html 之前加载，
   主页的 initPage() 会读取这些全局变量。
   如果不想用远程模式，把 API_MODE 改回 'config' 即可。 */
